/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dev.enola.app;

import dev.enola.list.LinkedList;

import static dev.enola.utilities.StringUtils.join;
import static dev.enola.utilities.StringUtils.split;
import static dev.enola.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
